'use client';
import React from 'react';

const StepHeader = ({ activeStep }: unknown) => {
  return (
    <div className="flex flex-row gap-4 justify-center mt-10">
      <div>step wrapper</div>
    </div>
  );
};

export default StepHeader;
